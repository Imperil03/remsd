# Project Handoff

Дата: 2026-09-25

Репозиторий: https://github.com/Imperil03/remsd. Основная ветка — `main`. Последний коммит, меняющий сайт в текущем выпуске: [`ed6a81a`](https://github.com/Imperil03/remsd/commit/ed6a81a71538885747101a6ac7041a2b50545019). Последующие коммиты документации могут не менять опубликованную сборку.

## Итог последних правок

- `/o-kompanii/` использует утверждённые владельцем тексты из `src/data/internal-pages/o-kompanii.json`. Три карточки описывают направления работы, а не историю развития компании.
- Список возможностей ремонтной базы расположен под адресом; на компьютере — две колонки, на телефоне — одна, затем отдельный отступ до фотографий.
- У команды удалён вводный абзац. «Условия ремонта» — компактная секция с двумя ненумерованными пунктами: «Согласование работ и стоимости», «Документы и гарантия». Точные формулировки находятся в JSON страницы; удалённые вводные и этапы не восстанавливать.
- Третья карточка показателей: `1 000 м² / площадь ремонтной базы`. Это итоговая формулировка владельца от 25.09.2026, независимая от числа страниц марок. Остальные показатели: `10+`, `500+`, `7`. Контракт компоновки и адаптива — `docs/company-page.md`.
- Полный каталог сертификатов находится на `/sertifikaty/`; главная и компания показывают по два превью. Этот перенос сохранён при совмещении параллельных правок. Контракт — `docs/documents.md`.

Публикация `ed6a81a` подтверждена 25.09.2026: [GitHub Actions, запуск 36132476666, попытка 2](https://github.com/Imperil03/remsd/actions/runs/36132476666) завершил сборку и deployment успешно. HTML и CSS публичной страницы совпали с этим коммитом; показатель площади, отсутствие вводных абзацев и два пункта условий проверены в браузере на 320, 390, 721 и 1440 px. Открытые работы по сегодняшнему выпуску отсутствуют.

## Быстрый вход для нового агента

Читать в таком порядке:

1. `AGENTS.md` — правила работы и запреты.
2. `.agent/SEO_STRUCTURE.md` — публичный контур, семейства страниц и SEO-архитектура.
3. Этот файл — текущее состояние и ближайший рабочий контракт.
4. `docs/project-history.md` — почему проект пришёл к текущему состоянию.
5. Для дизайна: `docs/design-guideline.md` → `docs/design-system.md` → `DESIGN.md`.
6. `docs/repair-pages-wave-one.md` — общий ремонтный шаблон, перенос Word, цены и происхождение иллюстраций.
7. `docs/special-equipment-pages.md` — десять дочерних hub спецтехники, фотокарточки узлов, адресная редактура и цены.
8. Для реализации: `src/data/internal-pages/index.json`, отдельные PageDefinition, `tools/lib/internal-pages.js`, `src/templates/internal-page.html`, `assets/css/internal-pages.css`.
9. `docs/brand-pages.md` и `docs/brand-source-map.json` — 23 страницы марок, источники, отличия `brand-v1` и проверка сохранности материала Word.
10. `docs/company-page.md` — страница компании, условия ремонта, показатели, фотографии, превью документов и отложенная карта.
11. `docs/contact-page.md` — контакты отделов, проезд, банковские реквизиты, PDF и копирование.
12. `docs/documents.md` — отдельная страница сертификатов, общий каталог данных и превью главной и компании.

## Запуск на другом компьютере

Нужны Git и Node.js 22, который используется и в CI. Клонировать репозиторий в любой каталог:

```bash
git clone https://github.com/Imperil03/remsd.git
cd remsd
git switch main
npm ci
npx playwright install chromium
npm run verify
```

На Linux системные зависимости браузера устанавливаются командой `npx playwright install --with-deps chromium`, как в CI.

В существующей копии сначала проверить `git status`, затем получить изменения через `git pull --ff-only`. Если есть локальные правки или расходящаяся история, сохранить и совместить их; не заменять рабочую копию вслепую. Для параллельной задачи подходит отдельный worktree от актуальной `origin/main`.

Открыть корень клонированного репозитория в новом Codex и начать с `AGENTS.md` и этого файла. Переносить старые локальные пути, `node_modules`, `test-results` или историю чата не требуется. Зависимости устанавливаются из lock-файла, скриншоты и отчёты создаются проверками. Доступ для push в GitHub настраивается на новом компьютере отдельно; секреты в репозитории не хранятся.

Команды браузерной проверки, production-like Lighthouse и возврата `dist/` в preview находятся в `README.md`. Для проверки компании в PowerShell:

```powershell
$env:BROWSER_ROUTES = "o-kompanii"
npm run test:browser
Remove-Item Env:BROWSER_ROUTES
```

В Bash эквивалент: `BROWSER_ROUTES=o-kompanii npm run test:browser`. Если меняются общие документы, добавить маршрут `sertifikaty`. После изменения hero, показателей или шрифтов выполнить `npm run build`, `npm run generate:critical`, `npm run verify`. В коммит входят исходники и обновлённый preview `dist/` без sitemap.

## Навыки при продолжении

Если соответствующие навыки доступны: `ru-text` — для русского текста, `impeccable` — для адаптива и компоновки, `seo-page` или `seo-technical` — для отдельной SEO-задачи. Они дополняют правила репозитория; утверждённые формулировки и дизайн из файлов проекта имеют приоритет. Отсутствие внешнего навыка не делает контекст проекта недоступным: основные требования записаны в `AGENTS.md` и `docs/`.

## Текущий статус

Две волны страниц ремонта реализованы на общем источнике данных. Публичная сборка содержит:

- утверждённую главную `/`;
- эталонный hub `/remont-gruzovyh-avtomobiley/`;
- `/remont-sedelnyh-tyagachey/` — ремонт седельных тягачей, `hub`.
- `/remont-polupricepov-i-tralov/` — ремонт полуприцепов и тралов, `hub`.
- `/remont-avtobusov/` — ремонт автобусов, `hub`.
- `/remont-spectehniki/` — ремонт спецтехники, `hub`.
- `/kuzovnoy-remont-gruzovoy-tehniki/` — кузовной ремонт грузовой техники, `service`.
- десять дочерних hub спецтехники: бульдозеры, экскаваторы, фронтальные погрузчики, автокраны, манипуляторы, самосвалы, трубоукладчики, грейдеры, дорожные катки и тракторы (маршруты в manifest и `docs/special-equipment-pages.md`);
- 23 страницы марок `/remont/{марка}/` на `brand-v1`;
- `/o-kompanii/` семейства `company`;
- `/kontakty/` семейства `contact`;
- `/sertifikaty/` семейства `documents`; всего 42 внутренние страницы в сборке;
- `404.html`.

Старые удалённые страницы не восстанавливались. Пятнадцать новых маршрутов созданы из двух предоставленных Word на утверждённой композиции эталона. Word не участвует в сборке.

Брендовые страницы созданы заново из «Бренды.docx», включая КАМАЗ, МАЗ и УРАЛ. Шаблон сохраняет 11 смысловых блоков, добавляет `modelRange`, `costEstimate` и необязательные `serviceGrid.items[].details`. Ссылки каталога и меню определяются по `entityRef` опубликованных PageDefinition через единый `tools/lib/brand-catalog.js`. Текущая марка в каталоге обозначается статически. Эталон `referenceByFamily.brand` — `remont/kamaz`.

## Эталонная внутренняя страница

Страница ремонта грузовых автомобилей использует визуальный язык главной: тёмный фото-hero, утверждённую типографику, фотографии базы, сдержанный жёлтый акцент, большой межсекционный ритм и общую шапку. Композиция включает:

1. intro/proof split с фотографией и четырьмя показателями;
2. шесть равноправных вертикальных карточек направлений ремонта в одной строке на wide desktop;
3. шестнадцать популярных работ у эталона и первой волны; 13–16 по Word у дочерних hub спецтехники;
4. шесть типов техники с фотографиями, сетка `3 × 2`; у десяти дочерних hub спецтехники — шесть ремонтируемых узлов в том же компоненте;
5. общий блок марок: три официальных карточки и матрица двадцати марок;
6. структурированный редакционный текст после марок;
7. шесть признаков у эталона, восемь на новых страницах, с правым CTA;
8. пять общих этапов, последний — «Выдача техники»;
9. две таблицы цен с правым CTA;
10. восемь связанных направлений с иконками, пояснениями и ссылками только на опубликованные маршруты; исключение — каталог десяти направлений у родительского hub спецтехники;
11. split-FAQ: вводная колонка и вторичная ссылка на мастера слева, 11 вопросов у эталона, 10 у первой волны и 5 у дочерних hub спецтехники справа.

После 11 секций — общий по оформлению контактный пролог футера с тематическим текстом.

Оформление смысловых блоков следует сохранённому референсу `docs/references/internal-hub-block-reference.png`, но его плотность, брендинг и реквизиты не копируются. Межсекционный ритм остаётся более свободным по токенам РемСД. Асимметричная фотомозаика, тёмный индекс и тёмная стартовая карточка этапов не используются. Desktop-направляющая страницы, общей шапки и футера — `1312 px` с полями `36 px`; на `≤720 px` сохраняется направляющая до `520 px`. Этапы используют пять колонок только на `≥1280 px`, ниже — вертикальный маршрут без разрыва слов. Две предусмотренные inline CTA находятся в блоках признаков и цен; FAQ использует только вторичную текстовую ссылку. Форм и боковой панели нет. Все действия звонка ведут на `tel:+79224488822`.

Иерархия визуальных источников:

1. утверждённая главная задаёт бренд, типографику, цвет, шапку, футер и общий уровень воздуха;
2. сохранённый референс задаёт порядок и оформление смысловых блоков эталонного hub;
3. живая страница `/remont-gruzovyh-avtomobiley/` является текущей принятой реализацией этого сочетания;
4. прежние варианты `/remont/` и отклонённая версия с фотомозаикой не являются источниками дизайна.

## Данные и факты

В `site-config.json` зафиксированы реквизиты РемСД, график `ежедневно с 08:00 до 22:00` и подтверждённые владельцем факты. Старые тарифы эталона от 29.08.2026 сохранены. Новые цены и оценки от 06.09.2026 отделены от них в `docs/repair-pages-wave-one.md`. Ориентиры второй волны от 07.09.2026 согласованы отдельной матрицей, см. `docs/special-equipment-pages.md`. Показатели: 10+, 500+, 80%, 1–2 дня — срок многих типовых работ.

Структурированные данные ремонтной страницы: `BreadcrumbList`, `Service`, `LocalBusiness`/`AutoRepair`. У компании используются `AboutPage`, `BreadcrumbList`, `Organization` и `LocalBusiness`/`AutoRepair`, без `Service`. Маркетинговые показатели в JSON-LD не включаются.

## Дизайн-система

- `design-system.css` — токены;
- `styles.css` — минимальный foundation;
- `site-chrome.css` — общая навигация, кнопка, callbar, шапка, футер и служебная 404;
- `styles-v3.css` — только главная;
- `internal-pages.css` — страницы ремонта и марок;
- `company-page.css` — страница компании.

Публичные бандлы: `base.css`, `home.css`, `internal.css`, `company.css`, `contact.css`, `certificates.css`. Контракт проверяет `tools/check-design-system.js`. Главная, страницы ремонта, компания, контакты и сертификаты имеют собственный critical CSS; эквивалентные объявления шрифтов не повторяются в отложенном бандле. `document-ui.css` входит в бандлы компании и сертификатов.

`src/data/internal-pages/index.json` — manifest схемы v3 с `referenceByFamily`; каждая страница хранится отдельным JSON. Семейства — `hub`, `service`, `brand`, `company`, `contact`, `documents`. 16 страниц ремонта используют `template: "repair-v1"`, а 23 страницы марок — `brand-v1`; оба шаблона содержат по 11 смысловых секций. Общие hero-факты, показатели, марки, этапы и контакт FAQ задаются только в `page-templates.json`. Компания не использует ремонтный шаблон и `metadata.serviceType`: её шесть внутренних секций вместе с hero и контактами образуют восемь блоков. Документы имеют только компактный hero и каталог, разметку `CollectionPage` и существующую сущность `proof-certificates`. Сущности и обе стороны отношений валидируются до сборки.

## Как добавлять следующую страницу

1. Определить интент и семейство страницы.
2. Подтвердить факты и текст по `.agent/SEO_STRUCTURE.md`.
3. Для аналогичной ремонтной страницы взять исходник с `template: "repair-v1"`, не копировать общие данные. Добавить отдельный `PageDefinition` в `src/data/internal-pages/` и включить его в manifest.
4. Переиспользовать существующие секции; новый тип секции вводить только для повторяемой задачи.
5. Сделать ссылку интерактивной только после появления маршрута в сборке. Количества работ и FAQ брать из утверждённого материала, не доводить искусственно до эталона. Для отдельной волны добавить приватный контентный fixture; общий PageDefinition не менять.
6. Выполнить `npm run build`, `npm run generate:critical`, `npm run verify`, `npm run test:browser`, `npm run test:lighthouse`, `git diff --check`.

Не создавать тонкие страницы формата «услуга × марка» без отдельного решения и достаточной фактуры.

## Проверка текущего выпуска

Правки компании от 25.09.2026: `npm run verify` прошёл для 44 HTML и шести CSS-бандлов. Компания проверена в браузере на 12 ширинах 320–1992 px; после совмещения с каталогом также проверялись сертификаты. Отдельно проверены длинное значение площади на 320 и 721 px, отсутствие обрезания, точные формулировки и два пункта условий без нумерации. Critical CSS показателей пересобран. CSS компании использует версию `20260925-certificates-page-v22-repair-terms`.

Первый запуск CI для `ed6a81a` прошёл браузерные тесты; Lighthouse компании дал 99/100/100/100, CLS 0. Выпуск остановил единичный замер неизменённой `/remont-greyderov/`: Performance 92 при пороге 95 и TBT 313 ms. В предыдущем успешном CI и адресной локальной перепроверке эта страница получила 99, TBT 0. Повторная попытка того же workflow прошла без изменения кода и порогов: все 43 маршрута прошли Lighthouse, грейдеры получили 99, TBT 20 ms; компания — 99/100/100/100, CLS 0. При похожем сбое сначала читать лог конкретного маршрута и сверять diff, затем перепроверять; не ослаблять проверки ради публикации.

Выпуск отдельной страницы документов от 25.09.2026: cache-buster `20260925-certificates-page-v22`. Пункт «Сертификаты» и футер ведут на `/sertifikaty/`; главная и компания показывают по два превью. Полный каталог — пять комплектов в трёх группах. Источник `src/data/documents.json`; viewer использует стабильные `documents-{id}`, сертификат соответствия включает шесть листов. Старые якоря документов компании перенаправляются на новую страницу. Адресный браузерный прогон прошёл 12 ширин и переходы/клавиатуру; Lighthouse сертификатов 99/100/100/100, CLS 0. Итого 44 HTML, шесть CSS-бандлов, 43 содержательных маршрута для Lighthouse. Одобренные тексты компании из ad6efb7 сохранены.

Выпуск контактов от 25.09.2026: cache-buster `20260925-contacts-v19`, пять CSS-бандлов, 43 HTML вместе с главной и 404. Контакты проверяются на 12 ширинах, Lighthouse — на 42 содержательных маршрутах. Общий пункт «Контакты» теперь ведёт на `/kontakty/`; старый якорь контактного блока главной сохранён. Реквизиты двух банков и PDF основаны на карточке, предоставленной владельцем. После изменения реквизитов запускать `npm run prepare:contacts`: сборка проверяет совпадение исходных данных и PDF по хешам.

Предыдущий выпуск компании: `20260924-company-v18`. Browser-контракт проверяет 11 ширин для `repair-v1` и пяти характерных марок, остальные марки — на 1440/390 px. Для компании добавлена 1121 px к базовым 11 ширинам. Lighthouse охватывает главную и все 40 внутренних маршрутов. Сохраняются пороги качества и noindex-превью без sitemap. Источники 26 WebP-иллюстраций узлов — `docs/repair-page-image-sources.json`.

Страница компании имеет восемь блоков вместо 11 секций ремонта. Ссылка добавлена после аренды в меню, в футер и блок базы главной. На 1121–1280 px скрыты адрес и email в шапке; телефон и меню остаются в строку, burger включается на 1120 px. Реальные фото открываются в нативном диалоге со стрелками, удержанием фокуса и возвратом к исходной ссылке; превью документов ведут на отдельную страницу сертификатов. Контакты используют общий график 08:00–22:00; карта загружается только возле нижнего блока. Данные и источники — `docs/company-page.md`.

Исправление от 23.09.2026: якорные ссылки меню («Сертификаты», «Контакты») не получают `aria-current="page"` по совпадению pathname. На главной нет автоматически выбранного пункта; внутренние маршруты сохраняют выделение своей ссылки и группы «Ремонт». Browser-контракт проверяет также `index.html`, префикс `/remsd/` и прямой вход с якорем на desktop/mobile.

## Репозиторий и история

`main` — источник актуального кода и документации. Он может опережать опубликованный сайт, пока workflow выполняется или завершился ошибкой. Публичная версия определяется последним успешным deployment и проверкой URL. Выпуски выполняются по очереди; полный прогон обычно занимает около 13–15 минут, это наблюдение, а не гарантированный срок. Статус можно проверить через `gh run list --repo Imperil03/remsd --branch main` и `gh run view <run-id> --repo Imperil03/remsd`.

История решений и ранних рабочих веток приведена в `docs/project-history.md`. При продолжении разработки ориентироваться на файлы из актуальной `main`, а историю использовать для понимания причин решений, а не для восстановления удалённых шаблонов. В конце сегодняшних правок новых продуктовых задач от владельца не осталось; дальнейшие изменения — по следующему заданию.
